package behavior.state.remote;

public class Main {
    static void main() {
        RemoteControl remote = new RemoteControl();
        remote.powerPress();
        remote.powerPress();
        remote.powerPress();
    }
}
